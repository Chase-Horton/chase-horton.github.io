---
title: "Bitwise Operations"
date: "2026-01-03"
description: "Common bitwise operations in Go."
tag: "Go"
isPublished: true
---
# Bitwise Operations in Go
### Recieve mask of flag N
```go
mask := 1<<N
```
<br/>
### Recieve a full mask of 0b11111 of N flags
```go
fullMask := 1<<N - 1
```
<br/>
### Check if flag is set
```go
if flags&mask > 0 {
    // set
}
```
<br/>
### Toggle a flag is set
```go
flags ^= mask
```
<br/>
### Get Lowest Bit
```go
lowestBit := flags & -flags
```
<br/>
### Check only 1 bit is set in any position
```go
if flags&(flags-1) == 0 {
    // only one bit is set
}
```
<br/>
### Iterate over Flags
```go
remainingBits := flags
for remainingBits != 0 {
    // get lowest bit
    maskWithLowestBit := flags & -flags
    // flip off lowest bit
    remainingBits ^= lowestBit
    // do work with mask
}
```
<br/>
### Sorting masks by # of flags set
```go
count := bits.OnesCount16(flags)
```